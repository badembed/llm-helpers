[project]
name = "big-md-bundler"
version = "0.1.0"
description = "Bundle source files into one Markdown and copy result to clipboard."
requires-python = ">=3.9"
dependencies = [
  "pyperclip>=1.9.0",
]

[project.scripts]
bundle-sources = "bundle_sources:main"
